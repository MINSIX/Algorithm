#include<bits/stdc++.h>
using namespace std;
int X, Y, D, T;

const double INF = 1e9;
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> X;
    while (X--)
        cout << 's';









}
